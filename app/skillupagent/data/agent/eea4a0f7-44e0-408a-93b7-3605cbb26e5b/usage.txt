This agent draws paths by delegating to the Path Agent sub-agent.

Example: "Draw a path on metal from (0,0) to (10,10) with width=3."

If the requested width is 3 or less, the agent automatically redraws it with width=4 to satisfy the minimum-width design rule, then reports the result.
